type: map
mapping:
   Parameters:
      include: parameter_map
   InputDataSets:
      type: seq
      sequence:
        - include: input_dataset
   OutputDataSet:
      include: output_dataset
   Initialize:
      include: initialization_block
   JobCommands:
      include: jobcommands_block
   Finalize:
      include: finalization_block 
     