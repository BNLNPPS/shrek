Parameters:
  name: top
  comment: "Simple chain from panda examples"
  nJobs: 10

OutputDataSets:
  - name: top/outDS
    filelist:
      - "regex|seed-*.txt"

JobCommands: |-
  echo $uniqueId > seed-`uuidgen`.txt


